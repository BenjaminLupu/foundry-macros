/* Generated on 20/09/2026 09:01:14
   GENERATED FILE: do not edit. Edit give-a-benny-to-connected-players.js (or lang/*.json), then run: python build/build.py */

// ---- Translations: generated by build/build.py from lang/*.json, do not edit ----
const I18N_TEXTS = {
  "en": {"benny.everyone": "Everyone gets a Benny!"},
  "fr": {"benny.everyone": "Vous gagnez tous un Jeton !"}
};
const t = (key, vars = {}) => {
  const lang = String(game.i18n?.lang ?? "en");
  for (const code of [lang, lang.split("-")[0], "en"]) {
    const table = I18N_TEXTS[code];
    if (!table) continue;
    let text = table[key];
    if (text === undefined && vars.n !== undefined) {
      text = table[`${key}.${new Intl.PluralRules(code).select(vars.n)}`] ?? table[`${key}.other`];
    }
    if (text !== undefined) return text.replace(/\{(\w+)\}/g, (match, name) => name in vars ? String(vars[name]) : match);
  }
  return key;
};
// ---- End of translations ----
main();

async function main() {

    let players = game.users.players; // Get players among all users
    let actors = game.actors; // Get all actors
    let activePlayerActors = 0;

    // Parse players
    for (let player of players) {

        // Check if the player is currently connected to the world
        if (player.active) {

            // Parse actors
            for (let actor of actors) {

                // Get actor's owner IDs
                let ownerIds = Object.getOwnPropertyNames(actor.ownership);
                
                // Check if the connected player ID in the list of actor's owner IDs
                let belongsToThePlayer = ownerIds.includes(player.id);
        
                // If the actor belongs to the connected player
                if (belongsToThePlayer){

                    // Get actor's current number of bennies
                    let b = actor.system.bennies.value;

                    // Give a benny to actor (should refresh counters in the UI)
                    await actor.update({
                        "system.bennies.value": b + 1,
                      });
                    
                    // Count one new rewared actor (for later benny animation)
                    activePlayerActors++;
                }
            }
        }
    }

    // If Dice So Nice! module is installed
    // https://gitlab.com/riccisi/foundryvtt-dice-so-nice/-/wikis/API/Roll
    if (game.dice3d) {
        
        // Build a roll with a benny for each actor of connected players
        let r = await new Roll(activePlayerActors + "db").evaluate();
        
        // Show 3D roll benny animation for GM and connected players
        game.dice3d.showForRoll(r,game.user,true);
    }

    // The message is posted under the GM's assigned character, or under the GM's account name
    // if there is none (same convention as the private messages)
    const gmAlias = game.user.character?.name || game.user.name;

    // Show a message in chat to tell players that each of them win a benny
    ChatMessage.create({
        content: t("benny.everyone"),
        speaker: { alias: gmAlias }
    });

}